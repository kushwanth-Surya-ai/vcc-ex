fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
# Both dialects are supported; DATABASE_URL decides which one is used.
aiosqlite>=0.19   # default - zero-setup local database
asyncpg>=0.29     # PostgreSQL
alembic>=1.13
pydantic[email]>=2.0
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
# passlib 1.7.4 reads bcrypt.__about__, which bcrypt removed in 4.1. Against
# bcrypt 5.x every password hash raises and ALL logins fail -- on PostgreSQL too.
# Pin until passlib ships a release that stops using the private attribute.
bcrypt<4.1
slowapi>=0.1.9
limits>=3.6
apscheduler>=3.10
httpx>=0.27
python-dotenv>=1.0
pytest>=8.0
pytest-asyncio>=0.23
aiofiles>=23.0
psutil>=6.0
# Training-only (port 8002 / training_worker.py). The live app on port 8000
# must never import this — see training_paths.py.
ultralytics>=8.3

